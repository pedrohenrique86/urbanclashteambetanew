{
  "name": "urbanclash-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Orquestrador principal para o projeto UrbanClash Team",
  "scripts": {
    "dev": "concurrently --names \"FRONT,BACKS\" --prefix-colors \"bgBlue.bold,bgGreen.bold\" \"npm run dev --prefix frontend\" \"npm run dev:all --prefix backend\"",
    "dev:all": "npm run dev:all --prefix backend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm start --prefix backend",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "dotenv": "^17.4.1"
  }
}